import test from 'node:test';import assert from 'node:assert/strict';import {evaluateHumanVishing} from '../human-vishing-evaluation.mjs';
const row=(id,label,extra={})=>({id,label,split:'test',topic:'bank',language:'en-IN',accent:'Indian English',channel:'PSTN',codec:'AMR-NB',voice_conversion:'none',...extra});
test('abstentions remain in class denominators',()=>{const r=evaluateHumanVishing([row('a','fraud'),row('b','genuine')],[{record_id:'a',prediction:'abstain'},{record_id:'b',prediction:'genuine'}]);assert.equal(r.overall.coverage,.5);assert.equal(r.overall.sensitivityIncludingAbstention,0);assert.equal(r.overall.specificityIncludingAbstention,1);});
test('missing, duplicate and non-test predictions fail visibly',()=>{const r=evaluateHumanVishing([row('a','fraud'),row('train','fraud',{split:'train'})],[{record_id:'a',prediction:'fraud'},{record_id:'a',prediction:'fraud'},{record_id:'train',prediction:'fraud'}]);assert.ok(r.errors.some(e=>/duplicate/.test(e)));assert.ok(r.errors.some(e=>/not for a sealed-test/.test(e)));});
test('subgroups retain topic and channel-specific confusion',()=>{const r=evaluateHumanVishing([row('a','fraud'),row('b','genuine',{topic:'it',channel:'WebRTC',codec:'Opus'})],[{record_id:'a',prediction:'genuine'},{record_id:'b',prediction:'fraud'}]);assert.equal(r.subgroup.topic.bank.fn,1);assert.equal(r.subgroup.channel.WebRTC.fp,1);});
